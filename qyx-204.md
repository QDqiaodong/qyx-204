项目名称：矿山职工宿舍公共洗漱台楼栋居住单元关联匹配系统
项目路径：/Users/Admin/Desktop/solo-0601/qyx-0701/qyx-组1/qyx-204

## 访问地址
- 前端：http://localhost:8124
- 后端API：http://localhost:8134
- MySQL：127.0.0.1:3350
- Redis：127.0.0.1:6423

## 启动命令
```bash
# 前端单独构建
cd frontend && npm ci && npm run build

# 后端单独编译
cd backend && mvn compile -q

# Docker整体启动
docker compose up -d --build

# 停止服务
docker compose down -v
```

## 验证结果
### 后端单独编译
✅ 通过 - mvn compile -q 无错误输出

### 前端单独构建
✅ 通过 - npm run build 构建成功，生成dist目录

### Docker整体构建启动
✅ 通过 - docker compose up -d --build 成功启动所有容器

### localhost与127.0.0.1验证
✅ 通过 - 两个地址返回同一项目页面

### 浏览器页面验证
✅ 通过 - 页面正常加载，中文显示正常

### 核心功能验证
✅ 洗漱台管理（CRUD）
✅ 居住单元管理（CRUD）
✅ 绑定匹配功能
✅ 容量校验逻辑（匹配/预警/不匹配）
✅ 匹配一览
✅ 校验记录查询
✅ 分表存储（按楼栋分表）
✅ MySQL二进制日志关闭

## 技术栈
- 前端：Vue3 + Vite + TypeScript + Element Plus
- 后端：Spring Boot 3.3 + JDK 17 + MyBatis Plus
- 数据库：MySQL 8.0（关闭二进制日志）
- 缓存：Redis 7
- 容器：Docker + Docker Compose
